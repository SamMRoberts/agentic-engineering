# Assumption Gate

## Task
Create the `agent-on-ramp-coach` plugin.

## Scope
Only the new `plugins/agent-on-ramp-coach` package, generated marketplace metadata, and local session gate artifacts.

## Non-goals
- Do not change existing plugin behavior.
- Do not add external dependencies.
- Do not create a backend service, MCP server, MCP app, or desktop app.
- Do not install a personal marketplace entry.

## Assumptions
- A1: The plugin belongs under `plugins/agent-on-ramp-coach`, not `~/plugins`. Verified by `plugins/AGENTS.md` and existing plugin directories.
- A2: Repo-compatible companion manifests and package metadata are needed beyond the user-listed minimum tree. Verified by `plugins/AGENTS.md` and existing plugin manifests.
- A3: `plugins/marketplace.json` is generated by `scripts/sync-marketplace.mjs`. Verified by the plugin guide and sync script.
- A4: The requested deterministic tooling can use Node built-in modules only. Verified from requested behavior and repository script conventions.

## Disproven Assumptions
None.

## Implementation Decision
Proceed. No high or critical assumption is unknown, and all medium-risk assumptions are verified with repository evidence.

## Final Verification
- `npm test` passed in `plugins/agent-on-ramp-coach`.
- `npm run validate` passed in `plugins/agent-on-ramp-coach`.
- The invalid adoption-session example failed validation as expected.
- Plugin Creator validation passed with PyYAML supplied through temporary `PYTHONPATH=/private/tmp/onramp-pyyaml`.
- `node scripts/sync-marketplace.mjs --check` passed after sync.
- Root `init`, `snapshot`, and `no-edits` passed.
- A simulated tracked-file edit caused `no-edits` to fail as expected, then passed after removing the probe.

## Open Risks
None currently known.
