{
  "name": "agent-compat",
  "version": "0.1.0",
  "description": "Convert Copilot *.agent.md custom agents into Codex and Claude instruction overlays.",
  "type": "module",
  "private": true,
  "bin": {
    "agent-compat": "./bin/agent-compat.mjs"
  },
  "scripts": {
    "test": "node --test test/**/*.test.mjs",
    "validate": "node bin/agent-compat.mjs validate --root ../.."
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "yaml": "^2.6.1"
  }
}
