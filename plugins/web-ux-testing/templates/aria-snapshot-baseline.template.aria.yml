# Example ARIA snapshot baseline.
# Keep this focused on stable roles, names, and states.
# Prefer regex for dynamic names and avoid private user data.

- heading "<Page or component title>" [level=1]
- navigation "<Primary navigation>":
  - link "<Stable link name>"
- main:
  - region "<Stable region name>":
    - button "<Stable action name>"
