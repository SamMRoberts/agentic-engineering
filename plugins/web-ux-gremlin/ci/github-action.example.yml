name: UX Gremlin

on:
  pull_request:
  workflow_dispatch:

jobs:
  web-ux-gremlin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Validate UX Gremlin plan
        run: node skills/web-ux-gremlin/scripts/web-ux-gremlin.mjs check
      - name: Enforce UX Gremlin coverage
        run: node skills/web-ux-gremlin/scripts/web-ux-gremlin.mjs coverage
      # CI is the stronger enforcement layer. After replacing placeholder locators,
      # uncomment the steps below to run the spec, ingest results, and gate merges
      # on suspected regressions.
      # - name: Run UX Gremlin Playwright spec
      #   run: npx playwright test .agent/generated/web-ux-gremlin.spec.ts --reporter=json > playwright-report.json
      # - name: Ingest results
      #   run: node skills/web-ux-gremlin/scripts/web-ux-gremlin.mjs ingest --input playwright-report.json --out .agent/session/web-ux-gremlin-results.json
      # - name: Report
      #   run: node skills/web-ux-gremlin/scripts/web-ux-gremlin.mjs report --results .agent/session/web-ux-gremlin-results.json
      # - name: Severity gate
      #   run: node skills/web-ux-gremlin/scripts/web-ux-gremlin.mjs gate --results .agent/session/web-ux-gremlin-results.json --fail-on high
