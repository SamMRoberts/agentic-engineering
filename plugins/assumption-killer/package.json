{
  "name": "assumption-killer",
  "version": "0.1.0",
  "description": "Cross-agent assumption gate workflow for implementation safety.",
  "type": "module",
  "private": true,
  "bin": {
    "assumption-gate": "./bin/assumption-gate.mjs"
  },
  "scripts": {
    "test": "node --test test/**/*.test.mjs",
    "validate": "node bin/assumption-gate.mjs check --root test/fixtures/valid-workspace",
    "summary": "node bin/assumption-gate.mjs summary --root test/fixtures/valid-workspace"
  },
  "engines": {
    "node": ">=20"
  }
}
